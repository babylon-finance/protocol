const { from, eth, parse } = require('../../lib/helpers');

const { ONE_DAY_IN_SECONDS, ONE_ETH, NOW, PROTOCOL_FEE, PROFIT_PROTOCOL_FEE } = require('../../lib/constants.js');

function takeProtocolFee() {}
